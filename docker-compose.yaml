version: '3.8'

services:
  game-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: game-server
    container_name: game-server
    ports:
      - "8080:8080"
      - "5005:5005"
    restart: unless-stopped
